# The Github instructions

```
git init (do this only the first time)
git add README.md
git commit -m "first commit"
git remote add origin git@github.com:rmvengen/test-project.git (do this only the first time)
git push -u origin master

```